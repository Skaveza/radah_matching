{
  "scripts": {
    "test:backend": "cd backend && ./vendor/bin/phpunit --testdox",
    "test:frontend": "cd frontend && npm run test:run",
    "test": "npm run test:backend && npm run test:frontend"
  }
}
